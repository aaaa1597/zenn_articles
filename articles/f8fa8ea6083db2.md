---
title: "React+TypeScriptでWebAssembly012。C++,Wasm,ReactTs。QRコード読み込み実装。"
emoji: "📖"
type: "tech"
topics: []
published: false
---

---
<- [React+TypeScriptでWebAssembly011](https://zenn.dev/rg687076/articles/96792e015ad528)
&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;[React+TypeScriptでWebAssembly013](xxxxxxx) ->

![](https://storage.googleapis.com/zenn-user-upload/6d8967181385-20240130.png =250x)

# Abstract
WebAssemblyで、QRコード読み込みを実装してみた。

# 結論
今回の成果物はココ↓
https://github.com/aaaa1597/ReactTs-WebAsm012

# 前提
- WebAssemblyのビルド環境、構築済み。[[環境構築]Ubuntu22.04で、WebAssemblyのOpenCV,C++,CMakeの開発環境を構築してみた。](https://zenn.dev/rg687076/articles/49f1d420820500)
- React+Typescriptの開発環境は構築済 [[環境構築]WindowsにVSCode+React+TypeScriptの開発環境を構築してみた。](https://zenn.dev/rg687076/articles/491d01e35cbce7)
- ベースはこれ→[reacttscppwasm_tmplate](https://github.com/aaaa1597/reacttscppwasm_tmplate)。

# 1.React + TypeScriptでカメラ起動の実装
## 準備
windowsだよ。

## 1-1.テンプレートのソースコード一式を取得
```shell: github取得
$ cd D:\Products\React.js\
$ git clone https://github.com/aaaa1597/reacttscppwasm_tmplate.git
$ ren reacttscppwasm_tmplate ReactTs-WebAsm012
$ cd D:\Products\React.js\ReactTs-WebAsm012
```

## 1-2. npm installを実行。
```shell: npm install
$ cd D:\Products\React.js\ReactTs-WebAsm012/reactts
$ npm install
```
## 1-3. VSCodeで開く。
ReactTs-WebAsm012\reacttsフォルダで右クリック。
![](https://storage.googleapis.com/zenn-user-upload/fe1d5a9beef2-20240201.png)

## 1-4. 一旦、実行。
```shell: 
$ cd D:\Products\React.js\ReactTs-WebAsm012/reactts
$ npm start
```
![](https://storage.googleapis.com/zenn-user-upload/99d26b132781-20240201.png)
動くのを確認。

## 1-5. App.tsx修正
変更点だけ。
C++で、detect(UInt8Array, int, int)のQRコードを検出する関数を作る予定で、その関数を呼び出す。
現時点では、再度Wasmビルドをするまで動かない。エラーになる。
```diff js: App.tsx
 65:  if(ctx && video && canvas && wasm) {
 66:  ctx.drawImage(video, 0, 0);
 67:  const data = ctx.getImageData(0, 0, canvas.width, canvas.height);
+68:  wasm.detect(data.data, data.width, data.height);
-68:  const buffer = wasm._creata_buffer(data.data.length);
-69:  wasm.HEAPU8.set(data.data, buffer);
-70:  wasm._Convert(buffer, data.width, data.height, cnt);
-71:  wasm._destroy_buffer(buffer);
-72:  cnt++;
-73:  if(cnt>=200) cnt = 0;
 74:}
 75:loopRef.current = requestAnimationFrame(loop);
 76:}
```

# 2.C++で、QRコード検出処理を実装
## 準備
windowsだよ。

## 2-1. VSCodeで開く。
C++開発もVSCodeを使う。VSCode便利～。
ReactTs-WebAsm012\cppフォルダで右クリック。
![](https://storage.googleapis.com/zenn-user-upload/76af8392de8f-20240201.png)
VSCodeの拡張機能C/C++、CMake Toolsを入れてなければ入れておく。
　　　　　　↓
- C/C++: Edit Configurations(JSON)を選択
Ctrl + Shift + P -> "C/C++: Edi"まで入力と出てくる。
![](https://storage.googleapis.com/zenn-user-upload/ffc3f8959f05-20240114.png)
　　　　　　↓
c_cpp_properties.jsonが生成される。ついでに右下にビルド/デバッグ/実行ボタンも表示される。
![](https://storage.googleapis.com/zenn-user-upload/2ad01be0bb68-20240114.png)

:::message
右下にビルド/デバッグ/実行ボタンも表示されない場合、
手順7を実行すると表示されるので問題ない。
:::

### 2-2. CMakeの設定
Ctrl + Shift + P -> "CMake: Configure"を選択。
X86を選ぶ。(WebAssemblyは32bitなので。)
![](https://storage.googleapis.com/zenn-user-upload/34219c0f6578-20240115.png)

### 2-3. インクルードパスを追加
ifcpp.cppに戻ると、赤波線でエラーになっている。
![](https://storage.googleapis.com/zenn-user-upload/548cb647bdb3-20240121.png)
　　　　　　↓
手順3でビルドしたパスを設定。
D:\apps\opencv\opencv-4.x\build\install\include
　　↑ ここは自分のOpenCVインストールフォルダに読み替える。
![](https://storage.googleapis.com/zenn-user-upload/64e850a31763-20240121.png)
　　　　　　↓
赤波線が消えている。
![](https://storage.googleapis.com/zenn-user-upload/71b2f274f390-20240121.png)

### 2-4. 一旦、実行
ブレークポイントを設定して、カブト虫ボタンを押す。
![](https://storage.googleapis.com/zenn-user-upload/23de86e57ae9-20240121.png)
　　　　　　↓
いろいろ動き出す。
![](https://storage.googleapis.com/zenn-user-upload/aaa7c3c91c2c-20240114.png)
　　　　　　↓
ちょっと待つと、ブレークポイントで止まる。
![](https://storage.googleapis.com/zenn-user-upload/42e8153ccf3a-20240121.png)
　　　　　　↓
再開。
![](https://storage.googleapis.com/zenn-user-upload/40823221c592-20240201.png)
動くのを確認。

## 2-5. QRコード準備
QRコードの準備。以下のQRコードを使う。
正解文字列 : "http://000111222"
![](https://storage.googleapis.com/zenn-user-upload/5f7fb9d91812-20240128.png)

## 2-6. QRコード読込み
変更点だけ。
```diff cpp: ifcpp.cpp
-6: std::string infilepath = "../../QR_sample6.png";
+6: std::string file = "../../QR_sample6.png";
+7: cv::Mat inputImage = cv::imread(infilepath);
～略～
-14: /* 画像処理 */
-15: cv::Mat grayscaleImage;
-16: ConvertColor(inputImage, grayscaleImage, cv::COLOR_BGR2GRAY);
-17: 
-18: /* 処理画像を表示 */
-19: cv::imshow("Grayscale Image", grayscaleImage);
-20: 
-21: /* キー入力待ち */
-22: cv::waitKey(0);
-23: 
-24: /* ウィンドウを閉じる */
-25: cv::destroyAllWindows();
+15: /* 計測開始 */
+16: std::chrono::steady_clock::time_point stime = std::chrono::steady_clock::now();
+17: 
+18: cv::Mat outputImage;
+19: bool ret = detect(inputImage, outputImage);
+20: 
+21: /* 計測終了 */
+22: std::chrono::steady_clock::time_point etime = std::chrono::steady_clock::now();
+23: long elapsedtime = std::chrono::duration_cast<std::chrono::milliseconds>(etime - stime).count() % 1000;
+24: std::cout << "elapsedtime: " << elapsedtime << "ms." << std::endl;
+25
+26: /* Output file. */
+27: int path_i = infilepath.find_last_of("/") + 1;
+28: int ext_i = infilepath.find_last_of(".");
+29: std::string pathname= infilepath.substr(0, path_i);
+30: std::string filename= infilepath.substr(path_i, ext_i-path_i);
+31: std::string extname = infilepath.substr(ext_i, infilepath.size()-ext_i);
+32: std::string outfile = pathname + "out" + filename + extname;
+33: cv::imwrite(outfile, outputImage);
```
## 2-7.実行
![](https://storage.googleapis.com/zenn-user-upload/44b4dc96300a-20240201.png)
outQR_sample6.pngが出力される。
![](https://storage.googleapis.com/zenn-user-upload/e715d06db776-20240201.png)
出来た!!
C++での実装、完了。

# 3. Wasmビルド
## 準備
ubuntuだよ。

## 3-1. 2-7まで開発したソースをubuntuに持っていく(共有フォルダとかで)
```shell: ReactTs-WebAsm012フォルダ移動
## Windowsから、Ubuntuの~/ReactTs-WebAsm012にコピー → フォルダ移動
$ cd ~/ReactTs-WebAsm012
```

## 3-2. 
```cpp: ~/ReactTs-WebAsm012/wasm/ifwasm.cpp
## Windowsから、Ubuntuの~/ReactTs-WebAsm012にコピー → フォルダ移動
$ cd ~/ReactTs-WebAsm012


```

## 2.ソースコードをWasmビルド
```shell: srcをビルド
$ cd ~/ReactTs-WebAsm012/wasm
$ mkdir build && cd build
$ emcmake cmake ..
$ emmake make
$ mv cppmain.js ..
$ mv cppmain.wasm ..
```

## 3.Webカメラの設定(内蔵カメラならこの手順は不要)
- Webカメラ挿して。
- Windowsの設定  [Windowsで設定](https://zenn.dev/rg687076/articles/8174f50cc2475a#3.windows%E3%81%A7%E8%A8%AD%E5%AE%9A)
- Virtual Box設定  [Virtual Box->デバイス->Webカメラ->HD Web Cameraにチェック](https://zenn.dev/rg687076/articles/8174f50cc2475a#4.vm%E3%81%A7%E3%80%81%E3%83%87%E3%83%90%E3%82%A4%E3%82%B9-%3Eweb%E3%82%AB%E3%83%A1%E3%83%A9-%3Ehd-web-camera%E3%81%AB%E3%83%81%E3%82%A7%E3%83%83%E3%82%AF)



## 10.サーバ起動
```shell: サーバ起動
$ cd D:\Products\React.js\ReactTs-WebAsm011\reactts
$ npm start
```

## 11.サーバにアクセス。
ブラウザから http://localhost:8080/ にアクセスする。

![](https://storage.googleapis.com/zenn-user-upload/6d8967181385-20240130.png)
出来た!!

func7()のJSオブジェクト, JSオブジェクト, JSオブジェクトの関数呼び出しがうまくできんかったけど、今のところ使う予定ないし、宿題ってことで。

---
<- [React+TypeScriptでWebAssembly010](https://zenn.dev/rg687076/articles/2d640f95404b4a)
&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;[React+TypeScriptでWebAssembly012](xxxxxxx) ->
