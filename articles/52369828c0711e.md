---
title: "React+TypeScriptなWebアプリで、R3Fのtutorial2.(Propsの使い方)"
emoji: "🚀"
type: "tech"
topics:
  - "react"
  - "typescript"
  - "threejs"
  - "props"
  - "r3f"
published: true
published_at: "2023-12-20 22:15"
---

![](https://storage.googleapis.com/zenn-user-upload/96d9c7ad549e-20231220.png =250x)

# Abstract
今回の参考は[ここ](https://sbcode.net/react-three-fiber/props/)。
propsのチュートリアルを動かしてみた。
props説明は[こっち](https://zenn.dev/ynakamura/articles/e562376735d398)が分かりやすい。

# 結論
今回の成果物はココ↓
https://github.com/aaaa1597/react-r3f-tutorial002

# 前提
- React+Typescriptの開発環境は構築済 [Ubuntu22.04+VSCode+React+TypeScriptの開発環境を構築してみた。](https://zenn.dev/rg687076/articles/5912e808924d28)
- 前回のプロジェクトから始める。[React+TypeScriptなWebアプリで、react-three-fiberのtutorialその1](https://github.com/aaaa1597/react-r3f-tutorial001)

# 手順
## 1.プロジェクト生成 -> VSCodeで開く
めんどいから、前回のプロジェクトから始める。[React+TypeScriptなWebアプリで、react-three-fiberのtutorialその1](https://github.com/aaaa1597/react-r3f-tutorial001)
で、下記コマンドでフォルダ名とか整備する。
```shell:フォルダリネームとか
$ NewProject=react-r3f-tutorial002
$ cd ~
$ git clone https://github.com/aaaa1597/react-r3f-tutorial001.git
$ rm -rf react-r3f-tutorial001/.git
$ rm -rf ${NewProject}
$ mv react-r3f-1ststep ${NewProject}
```

# App.tsx
まず全体。
```diff ts:App.tsx
import React from 'react';
import './App.css';
import { Canvas } from '@react-three/fiber'

+type Props = {
+ position?: number[];
+ name?: string;
+}

+const Box = (props: Props) => {
+ return (
+   <mesh {...props}>
+     <boxGeometry />
+     <meshBasicMaterial color={0x00ff00} wireframe />
+   </mesh>
+ )
+}

-function App() {
+const App = () => {
  return (
    <div style={{ width: "75vw", height: "75vh" }}>
-     <Canvas>
+     <Canvas camera={{ position: [3, 1, 2] }}>
        <ambientLight />
        <directionalLight />
-       <mesh>
-         <boxGeometry args={[4, 4, 4]}/>
-         <meshBasicMaterial color={0x00ff00} wireframe />
-       </mesh>
+       <Box position={[-0.75, 0, 0]} name="A" />
+       <Box position={[0.75, 0, 0]} name="B" />
      </Canvas>
    </div>
  );
}

export default App;
```

### propsに関するところを抜き出す。
propsの型を定義する。
propsって予約語かなんかかと思ったら、そんなことなく、普通に定義が必要みたい。
```diff ts:App.tsx
type Props = {
 position?: number[];
 name?: string;
}
```

propsの受け側。引数として定義する。ま、普通だね。
```diff ts:App.tsx
const Box = (props: Props) => {
```

propsの渡し側1。
"...props"、ドットのついたやつって引数に渡してたんだね。そのまま渡せるんだ。
<MyComponent {...info} />
// ↑の書き方は↓と同義なんだって。
<MyComponent name={info.name} age={info.age} gender={info.gender} />

```diff ts:App.tsx
    <mesh {...props}>
      <boxGeometry />
      <meshBasicMaterial color={0x00ff00} wireframe />
    </mesh>
```

propsの渡し側。
```diff ts:App.tsx
    <Box position={[-0.75, 0, 0]} name="A" />
    <Box position={[0.75, 0, 0]} name="B" />
```
なるほど～。

![](https://storage.googleapis.com/zenn-user-upload/96d9c7ad549e-20231220.png)
出来た!!

---
[React+TypeScriptなWebアプリで、R3Fのtutorial1.(コンポーネント化)](https://zenn.dev/rg687076/articles/9cc88dd8a7cb3a)

---
[React+TypeScriptなWebアプリで、R3Fのtutorial3.(useEffectとuseLayoutEffect)](https://zenn.dev/rg687076/articles/d6d5d103c14050)
