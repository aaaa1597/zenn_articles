---
title: "[環境構築]VirtualBox+Ubuntu22.04+VSCodeで、C++の開発環境を構築する。(ついでにOpenCVも)"
emoji: "📝"
type: "tech"
topics:
  - "cpp"
  - "vscode"
  - "virtualbox"
  - "ubuntu2204"
  - "観光構築"
published: true
published_at: "2023-10-21 17:48"
---

まとまった記事が探しても見つからなかったので、備忘録として。

# 前提
VirtualBox,Ubuntu22.04,VSCodeはインストールしておく。
1. [VirtualBoxインストール](https://qiita.com/HirMtsd/items/be1c8afe708e901e1100)
　　↓
2. [VirtualBox上でUbuntu22.04を構築](https://zenn.dev/rg687076/articles/795f4307117d35)
　　↓
3. [VSCodeをインストール](https://zenn.dev/rg687076/articles/70af339b8b5ba6)
　　↓
4. (必須じゃないけど)[VSCodeを日本語化](https://www.javadrive.jp/vscode/install/index4.html)

# 1.C++ビルド環境をVSCodeに構築する
```shell:必要なライブラリをインストール
$ sudo apt update
$ sudo apt upgrade -y
$ sudo apt autoremove -y
$ sudo apt -y install build-essential cmake
```
# 2.サンプルコードを実行してみる。
```shell:main.cppを動かす。
$ mkdir c++test && cd c++test
```
↓
VSCodeで、作成したフォルダを開く。
![](https://storage.googleapis.com/zenn-user-upload/c5f123b8d23f-20231126.png)
↓
信頼する。
![](https://storage.googleapis.com/zenn-user-upload/984d48806424-20231126.png)
↓
main.cppを作成
![](https://storage.googleapis.com/zenn-user-upload/744a272fa1d0-20231126.png)
↓
Ctrl+Sで保存して、名前をmain.cppにする。
下記内容で保存。
```cpp:main.cpp
#include <iostream>

int main(void) {
	std::cout << "Hello, VSCode!" << std::endl;
	return 0;
}
```
↓
ビルド -> 実行
![](https://storage.googleapis.com/zenn-user-upload/b81e310e8914-20231126.png)
↓
出来た!! 期待通り"Hello, VSCode!"が表示されている。
![](https://storage.googleapis.com/zenn-user-upload/ab08f09f12e9-20231126.png)
↓
ブレークポイントで止まったのも確認できた。
![](https://storage.googleapis.com/zenn-user-upload/052ac0f93b77-20231126.png)

# (補足)ついでに、OpenCVの開発環境も作ってみる。
- VSCodeにcmakeとcmake toolsをインストールする。
↓はすでにインストール済だけど、CmakeとCmake Toolsの2つをインストールする。
![](https://storage.googleapis.com/zenn-user-upload/db74b73ee3c0-20231126.png)
↓
- 必要なライブラリをインストール
- cmakeインストール(VSCodeとは別に必要らしい)
```shell:cmakeインストール
$ sudo apt -y install cmake
```
- libgtk2.0-devインストール(cv::imshow()を動かすのに必要)
```shell:libgtk2.0-devインストール
$ sudo apt -y install libgtk2.0-dev
```
- gitインストール
```shell:gitインストール
$ sudo apt -y install git
```
↓
- githubから一式取得
```shell:githubから一式取得
$ mkdir opencvtmp && cd opencvtmp
$ git clone https://github.com/opencv/opencv.git
```
↓
- 下記コマンド実行
```sh OpenCVソースをcmakeしてmakeしてinstall
$ cd opencv/
$ mkdir build && cd build
$ cmake -DOPENCV_GENERATE_PKGCONFIG=ON ..
$ cmake --build .
$ make 
$ sudo make install
$ sudo ldconfig
```
↓
- 適当にフォルダ作って移動
```sh
$ mkdir opencvtest && cd opencvtest
```
↓
- サンプルコード作成(main.cpp)
```cpp
#include <opencv2/opencv.hpp>

int main(void) {
    // 画像の読み込み
    cv::Mat inputImage = cv::imread("../input.png");

    // 画像が正しく読み込まれたかを確認
    if (inputImage.empty()) {
        std::cerr << "画像を読み込めませんでした" << std::endl;
        return -1;
    }

    // 画像の処理を行う ここでは例として、画像をグレースケールに変換する
    cv::Mat grayscaleImage;
    cv::cvtColor(inputImage, grayscaleImage, cv::COLOR_BGR2GRAY);

    // グレースケール画像を表示
    cv::imshow("Grayscale Image", grayscaleImage);

    // キー入力待ち
    cv::waitKey(0);

    // ウィンドウを閉じる
    cv::destroyAllWindows();
    return 0;
}
```

↓
- input.pngを準備
なんでもいいので画像を準備する。(このページのでもいい)

- CMakeLists.txt作成
```sh
cmake_minimum_required(VERSION 3.0)
project(OpenCVExample)

# OpenCVのパッケージを見つける
find_package(OpenCV REQUIRED)

# #OpenCV関係のインクルードディレクトリのパスを設定
# include_directories( ${OpenCV_INCLUDE_DIRS} )

# ソースファイルを指定
set(SOURCES main.cpp)

# 実行可能ファイルをビルド
add_executable(OpenCVExample ${SOURCES})

# OpenCVをリンク
target_link_libraries(OpenCVExample ${OpenCV_LIBS})
```

```sh
$ ls -l
合計 8
-rw-rw-r-- 1 jun jun 465 10月 21 22:15 CMakeLists.txt
-rw-rw-r-- 1 jun jun 743 10月 21 22:15 main.cpp
```
↓
VSCodeで、Shift+Ctrl+Pでパレット表示 -> CMake Configureを選択 -> "GCC 11.4.0 x86 ..." を選択
![](https://storage.googleapis.com/zenn-user-upload/66ae5fca6741-20231021.png)
↓
ビルド押下
![](https://storage.googleapis.com/zenn-user-upload/088a8df3f68c-20231021.png)
ビルドが終了コード 0 で終了しました
↓
画像(input.jpg)を準備して、
![](https://storage.googleapis.com/zenn-user-upload/9d37c415e4f3-20231021.png "=10px")

```
$ ls -l
合計 100
-rw-rw-r-- 1 jun jun   465 10月 21 22:15 CMakeLists.txt
-rw-rw-r-- 1 jun jun 88317 10月 21 22:34 input.jpg
rw-rw-r-- 1 jun jun   743 10月 21 22:15 main.cpp
```
↓
カブト虫マークを押下して、デバッグ開始
![](https://storage.googleapis.com/zenn-user-upload/be74c184814d-20231021.png)
:::message
↓↓↓　CMakeで実行するときは動かんみたい。
![](https://storage.googleapis.com/zenn-user-upload/f6189f6aa1f7-20240114.png)
:::
↓
無事、グレースケールになった画像が表示された。
![](https://storage.googleapis.com/zenn-user-upload/a874dd1f04da-20231021.png)
いえー！
